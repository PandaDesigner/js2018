/* Para pedir un dato al usuario se puede usar prompt(mensaje) */

//prompt("Introduzca su nombre");

/* Para imprimir un dato se puede usar console.log(dato) */

//console.log(dato);

/* Para pedir un dato al usuario se puede usar prompt(mensaje) */

let tuName = prompt("Introduzca su Nombre");
let age = prompt("Intrudzca su edad");

// Para imprimir un dato se puede usar console.log(dato)
newAge = parseInt(age) + 1;
console.log(`Hola ${tuName}, tines ${age} y el proximo año tendras ${newAge}`);
